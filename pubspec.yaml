name: custom_camera_component
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+30  # <== alza sempre questo numero quando ricompili iOS

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Fotocamera e salvataggio file temporanei
  camera: ^0.11.1
  path_provider: ^2.1.5

  # (opzionale) icone iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true