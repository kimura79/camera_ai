name: custom_camera_component
description: A new Flutter project.
publish_to: 'none'

# ↑ alza sempre il build number quando ricompili iOS
version: 1.0.0+2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # 📷 Fotocamera + filesystem temp
  camera: ^0.11.1
  path_provider: ^2.1.5

  # 🧩 Stato
  provider: ^6.1.5
  shared_preferences: ^2.2.3

  # 🎨 Font personalizzati (resta su 6.x)
  google_fonts: ^6.3.0
  from_css_color: ^2.0.0   # 👈 aggiunto per risolvere errore build

  # 🔑 Permessi runtime
  permission_handler: ^11.3.1

  # 💾 Salvataggio in Galleria (compatibile con http 1.x)
  image_gallery_saver: ^2.0.3

  # (opzionale) Icone iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true